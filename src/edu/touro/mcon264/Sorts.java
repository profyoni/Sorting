package edu.touro.mcon264;

public class Sorts
{
    public static <T extends Comparable<T> > void quickSort(T[] list) {
        //// TODO
    }

    /*****
     * returns the element with specified rank from the maximum. For example, if the <code>list</code> is
     * <code>[4,5,8,11]</code> and <code>rank</code> is 0, the maximum value (11) will be returned. If rank is 1,2 or 3
     * then 8, 5, or 4 will returned respectively.
     * @param list
     * @param rank
     * @param <T>
     * @return
     */
    public static <T extends Comparable<T> > T nthMax(T[] list, int rank) {
//// TODO
        return null;
    }

}